# Complete Optics Design Token Reference

**IMPORTANT: These are the ONLY valid token names. Do not invent token names like --op-space-600 or use hard-coded pixel values.**

{{CONTENT}}

---

**CRITICAL RULES:**

1. NEVER invent token names - only use names from this list
2. NEVER use hard-coded px values - always use tokens
3. For colors, use search_tokens tool to find available color tokens
4. Token names use words like 'small', 'medium', 'large', NOT numbers like '600'
